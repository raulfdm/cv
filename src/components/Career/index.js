import React from 'react';
import { Section, SectionBody, SectionTitle } from '../Section';

export default () => {
  return (
    <Section>
      <SectionTitle title="Carrer Summary" />
      <SectionBody>
        Front-end Developer with over 2 years of experience. Perform front-end developer tasks such
        as creating websites or web applications with css frameworks or from scratch, always based
        on UI/UX good practices and using or not Javascript Framework. Configure task runner to
        deploy, test and compiling and automate time-consuming job. Knowledge in development of
        RESTful API's using NodeJS and data persistence with mongodb or firebase.
      </SectionBody>
    </Section>
  );
};
